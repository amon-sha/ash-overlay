diff --git a/vmmon-only/linux/hostif.c b/vmmon-only/linux/hostif.c
index b33b1a7..4893623 100644
--- a/vmmon-only/linux/hostif.c
+++ b/vmmon-only/linux/hostif.c
@@ -3646,7 +3646,11 @@ HostIF_SetFastClockRate(unsigned int rate) // IN: Frequency in Hz.
       }
    } else {
       if (linuxState.fastClockThread) {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 3, 0)
+         force_sig(SIGKILL);
+#else
          force_sig(SIGKILL, linuxState.fastClockThread);
+#endif
          kthread_stop(linuxState.fastClockThread);
 	 close_rtc(linuxState.fastClockFile, current->files);
 
