diff --git a/src/util/Makefile.am b/src/util/Makefile.am
index 785916d..8c359b6 100644
--- a/src/util/Makefile.am
+++ b/src/util/Makefile.am
@@ -58,11 +58,8 @@ endif
 
 
 
-# actually required in multiple places, but good enough for now
-if ENABLE_USB_COND
 libutil_la_SOURCES +=      \
 device_id_util.c
-endif
 
 if ENABLE_UDEV_COND
 libutil_la_SOURCES +=      \
